- name: update servers
  hosts: wiki 
  become: yes
  tasks:
    - name: task ping
      ping:
    - name: update_cache
      apt:
        update_cache: yes
    - name: upgrade
      apt:
        upgrade: yes
